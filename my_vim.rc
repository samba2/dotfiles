" 'desert' for dark background, 'default' for bright
colorscheme desert
set smartindent
set tabstop=4
set shiftwidth=4
set expandtab
syntax on
set ruler
set showmode
"enable vim enhanced features, disables vi compatibility
set nocompatible

" for python editing
filetype indent plugin on

" --- browsing ---
" Hit enter in the file browser to open the selected
" file with :vsplit to the right of the browser.
let g:netrw_browse_split = 4
" vertical split instead of horizontal
let g:netrw_preview = 1 
let g:netrw_altv = 1
" tree style
let g:netrw_liststyle=3

" Change directory to the current buffer when opening files.
set autochdir
